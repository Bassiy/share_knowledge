# CSSセレクタ

## 概要
CSSルール（`セレクタ { 宣言 }`）のうち、「どの要素にスタイルを当てるか」を指定する部分。

## 理解したこと

### セレクタと宣言の2部構成
CSSのルールは「セレクタ（誰に）」と「宣言（何を）」の2部構成になっている。

```css
#box {        /* ← セレクタ：id=boxの要素を指す */
  color: red;  /* ← 宣言：どう変えるか */
}
```

セレクタが対象を絞り込み、宣言がその対象に適用するスタイルを決める。

---

### 複合セレクタと結合子の違い
セレクタをスペース無しで連結する書き方を複合セレクタ（compound selector）と呼び、「同じ要素が両方の条件を満たす」というAND条件を意味する。

スペースや`>`などの記号（結合子／combinator）を挟むと、「別の要素同士の関係」を表すようになる。

| 書き方 | 名前 | 意味 |
|---|---|---|
| `#box.active` | 複合セレクタ | 同じ要素が両方の条件を満たす（AND） |
| `#overlay.open #modal` | 子孫結合子（スペース） | `#overlay.open`の中にある`#modal`（何階層下でもよい） |
| `#overlay.open > #modal` | 子結合子（`>`） | `#overlay.open`の直接の子である`#modal`に限定 |

スペースの有無が「同じ要素への複数条件」か「別要素同士の関係」かを分ける境目になる。

---

## 関連概念
- dom（HTMLの入れ子構造がDOMの親子関係になるという前提があるからこそ、子孫結合子・子結合子で「親子関係」を指定できる）

## 関連実装
- [css_transition_basics](../coding/css_transition_basics/) — `#box.active`（複合セレクタ）と`#overlay.open #modal`（子孫結合子）の両方を実装で使い分けた

## ソース
- 2026-06-25・/codeセッションでの実装・対話から整理

## タグ
CSS, セレクタ, 複合セレクタ, 結合子, combinator
